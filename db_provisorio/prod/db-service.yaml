apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: jx-production
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-internal: "0.0.0.0/0"
spec:
  ports:
  - port: 5432
    nodePort: 31005
    targetPort: http-db
    protocol: TCP
  selector:
    app: db
  type: LoadBalancer
